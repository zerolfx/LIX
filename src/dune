(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags -la 2)
)

(executable
 (name repl)
 (public_name repl)
 (libraries
  core
  str
  llvm
  llvm.analysis
  llvm.executionengine)
 (preprocess (pps ppx_deriving.show)))